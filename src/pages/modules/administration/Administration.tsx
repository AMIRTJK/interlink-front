import { Outlet } from "react-router-dom";

// Layout раздела «Администрирование»: вложенные роуты (лендинг / подмодули)
export const AdministrationLayoutPage: React.FC = () => {
  return <Outlet />;
};
