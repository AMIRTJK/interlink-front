# Прогресс: паритет редактора с Word — трекер выполнения

> Рабочий чеклист к плану [2026-07-22-editor-word-parity-analysis.md](./2026-07-22-editor-word-parity-analysis.md).
> При команде «продолжай» — брать следующую невыполненную задачу отсюда, НЕ перечитывая анализ заново.

Файлы вмешательства:
- `src/widgets/CreateInternalCorrespondence/ui/CreateInternalCorrespondence.tsx` (5585 строк)
- `src/widgets/CreateInternalCorrespondence/lib/utils.ts` (735 строк)

Легенда статусов: ⬜ не начато · 🟨 в работе · ✅ готово · ⏭️ пропущено (по согласованию)

## Этап 1 (P0, каркас ввода)
- ✅ 4.9 Единая модель блока `<p>` (defaultParagraphSeparator=p; makeEmptyPara/insertPageBreak/applyHistoryState/handleEditorInput/wrapBareTopLevelNodes/removeSpacerSafely/paginate flushBuf → `<p>`; CSS `[&_p]:min-h-[1.8em] [&_p]:!my-0`)
- ✅ 4.1 Enter создаёт абзац `<p>` с наследованием формата (через defaultParagraphSeparator=p — нативный split наследует стиль)
- ✅ 4.2 Shift+Enter — стабильный мягкий перенос `<br>` (+якорный br в конце блока через caretAtBlockEnd)
- ✅ 4.3 Tab — контекстный (начало абзаца → text-indent шаг 1.27cm; список → execCmd indent; иначе **табулятор фикс. ширины** — `<span data-tab contenteditable=false style="display:inline-block;width:1.27cm">\t</span>` через `makeTabSpacer`). ⚠️ Багфикс: раньше вставлялся символ `\t` + `tab-size:1.27cm` — из-за выравнивания по сетке после набранного слова табуляция схлопывалась до пары пикселей («пробел»). Фикс. ширина даёт одинаковый шаг всегда. Backspace и Shift+Tab удаляют спейсер целиком (`nodeBeforeCaret`/`isTabSpacer`).
- ✅ 4.4 Shift+Tab — перехват (preventDefault, утечки фокуса нет); список → outdent; text-indent → уменьшение; иначе → удаление `\t` слева

## Этап 2 (P1, редактирование и буфер)
- ✅ 4.12 Свои copy/cut с очисткой (`handleEditorCopyCut`; cleanEditorArtifacts, text/plain через `htmlToPlainText`)
- ✅ 4.5 Не схлопывать значимые пробелы при вставке (sanitizeWordHtml, холст pre-wrap)
- ✅ 4.6 Backspace: своё слияние блоков с форматом приёмника (снят guard `if (!spacers.length) return` → берём на себя и обычные соседние абзацы через `mergeAcrossBoundary`; атомарные/hr отдаём дефолту)
- ✅ 4.7 Delete: зеркально 4.6 (направление next)
- ✅ 4.8 normalizeBlock — чистка пустых inline-обёрток (`EMPTY_INLINE_TAGS`; вызывается внутри `mergeAcrossBoundary` перед вычислением каретки)
- ✅ 4.13 Plain-вставка абзацами (split по \n{2,} → `<p>`, одиночный \n → `<br>`)
- ✅ 4.14 Списки: Tab/Shift+Tab уровень (было), Enter на пустом `<li>` — выход через `execCmd("outdent")`

## Этап 3 (P2–P3, полировка и «потолок»)
- ⬜ 4.15 Индикация кнопок по queryCommandState (selectionchange)
- ⬜ 4.10 Коррекция каретки на границах страниц (стрелки через spacer/page-break)
- ⬜ 4.11 Выделение: user-select:none на служебных узлах, коррекция границ
- ⬜ 4.16 Undo по границам слов (пробел/пунктуация/Enter → commitHistoryNow)
- ⬜ 4.17 Автоформат/типографика (по согласованию с заказчиком)

## Журнал сессий
<!-- Дописывать при каждой остановке: дата, что сделано, где остановился, следующий шаг, открытые вопросы -->
- 2026-07-22: создан трекер. Реализация ещё НЕ начата. Следующий шаг — 4.9 (единая модель `<p>`).
- 2026-07-22 (сессия 2): **Этап 1 выполнен полностью** (4.9, 4.1, 4.2, 4.3, 4.4). Все правки в `CreateInternalCorrespondence.tsx`. `tsc -b` чисто, eslint без новых замечаний в изменённых участках, dev-сервер собирается без ошибок. Ключевые места: helpers `closestLiOf`/`getTextIndentCm`/`deleteTabBeforeCaret`/`TAB_STEP_CM` добавлены рядом с `mergePageBlocks` (~строка 274); `handleEditorKeyDown` (~3084) — новые ветки Shift+Enter и контекстный Tab/Shift+Tab, в deps добавлен `execCmd`; useEffect styleWithCSS + `defaultParagraphSeparator=p`; JSX редактора — `tabSize:"1.27cm"` и классы `[&_p]:min-h-[1.8em] [&_p]:!my-0`. Замечание: старый Tab вставлял 4× U+00A0 (nbsp), а не пробелы — заменено на `\t`.
  - **Осталось для Этапа 1: ручная QA в Chrome и Firefox** (приложение за SSO — из песочницы редактор недоступен): проверить Enter→`<p>`, Shift+Enter, Tab в начале абзаца/в середине/в списке, Shift+Tab (нет утечки фокуса), регрессии пагинации/печати/экспорта/ЭЦП.
  - Следующий шаг при «продолжай» — **Этап 2, задача 4.12** (свои copy/cut с очисткой).
- 2026-07-22 (сессия 3): исправлены 4 бага ввода после Этапа 1 (все в `CreateInternalCorrespondence.tsx`):
  1. **Табулятор не выделялся при Ctrl+A** — со спейсера снят `contenteditable=false` (делал его атомарным островом, не попадавшим в подсветку выделения). Теперь `\t` внутри — обычный выделяемый текст. Атомарность удаления обеспечена обработчиками клавиш.
  2. **Space не работал в пустом редакторе** и 3. **Tab+Space «съедал» табуляцию** — в `handleEditorInput` проверка «документ пуст» использовала `textContent.trim()`, считая пробел/таб пустотой и стирая их сбросом на `<p><br></p>`. Заменено на `textContent.length` (пусто == нет символов вообще).
  4. **Tab увеличивал высоту строки** — inline-block-спейсер добавлял выносной элемент ниже базовой линии. Добавлен `vertical-align:top`.
  - Инфраструктура удаления: helpers `nodeAfterCaret`, `tabSpacerToDelete` (учитывает каретку слева/справа/внутри спейсера), `removeTabSpacer`. Backspace/Delete/Shift+Tab удаляют табулятор целиком одним нажатием.
  - `tsc -b` чисто, eslint без новых замечаний. **Проверка в браузере из песочницы невозможна** (SSO + localhost недоступен пейну + статические снапшоты непрозрачны) — нужна ручная QA под логином в Chrome/Firefox.
- 2026-07-22 (сессия 4): задача 1 (Ctrl+A) НЕ была решена в сессии 3, плюс появился баг переноса. Пересмотрел представление табулятора.
  - Диагноз: `\t` (даже внутри inline-block) — trailing/collapsible whitespace, выделение его НЕ подсвечивает → #1 оставался. А снятие `contenteditable=false` пустило каретку внутрь inline-block фикс. ширины → ввод переносился на новую строку внутри коробки (новый баг).
  - Решение: табулятор = прогон **неразрывных пробелов** (U+00A0) в `<span data-tab="1">` (без inline-block, без \t). Неразрывные пробелы всегда подсвечиваются (Ctrl+A), не переносятся, не меняют высоту строки, ширина постоянна. Кол-во nbsp считается по ширине пробела текущего шрифта через canvas (`tabNbspCount`), цель ≈1.27см. Атомарное удаление — прежними обработчиками по `data-tab`. Убран `TAB_WIDTH`; `tab-size:1.27cm` на контейнере оставлен для легаси-`\t`.
  - `tsc -b` чисто, eslint без новых замечаний. Ручная QA под логином всё ещё нужна (highlight/перенос/высота — визуальные).
- 2026-07-22 (сессия 5): **найдена НАСТОЯЩАЯ причина «табы не выделяются»** (сессии 3–4 чинили не ту ветку). Репро пользователя — «5×Tab из пустой строки → Ctrl+A». В коде Tab в начале абзаца шёл в ветку `text-indent` (CSS-красная строка), а НЕ во вставку спейсера. `text-indent` — это стиль, а не содержимое, поэтому в выделение он не попадает. Все правки сессий 3–4 меняли только ветку «иначе» (спейсер), которую этот репро вообще не задействовал.
  - Фикс: **убрана ветка `text-indent` в начале абзаца** (`caretAtBlockStart`→text-indent). Теперь Tab ВСЕГДА вставляет выделяемый табулятор (nbsp-спейсер), в т.ч. в начале строки — это и ближе к дефолту Word (Tab = символ табуляции, а не красная строка). При вставке в пустой блок убирается placeholder `<br>` (иначе лишняя пустая строка). Shift+Tab: сначала удаление спейсера, затем (легаси) уменьшение text-indent из импортных Word-документов.
  - Обоснование корректности категорично (не зависит от рендеринга): text-indent не даёт DOM-текста → Ctrl+A нечего подсвечивать; nbsp — это символы → подсвечиваются (пользователь сам подтвердил, что «пробелы выделяются»). `tsc -b` чисто. Живая проверка из песочницы невозможна (SSO, localhost недоступен пейну, Chrome MCP не подключён) — подтвердить визуально под логином.

- 2026-07-22 (сессия 8): **Этап 2 завершён.** Обнаружено, что 4.5/4.12/4.13 уже были в коммите `c6d98c9` (трекер отставал — обновил статусы). Доделал в этой сессии 4.6, 4.7, 4.8, 4.14 (все в `CreateInternalCorrespondence.tsx`):
  - **4.8** `normalizeBlock` + набор `EMPTY_INLINE_TAGS` (перед `mergePageBlocks`, ~строка 417): удаляет пустые inline-обёртки (`<b></b>`, `<span></span>` и т.п.) без текста и без img/br/hr/table, затем `normalize()`. Вызывается внутри `mergeAcrossBoundary` СРАЗУ после `mergePageBlocks` и ДО `charPosAt`, чтобы возвращаемая позиция каретки считалась по уже вычищенному DOM.
  - **4.6/4.7** В `handleEditorKeyDown` (Backspace/Delete) убран ранний `if (!spacers.length) return` — теперь берём на себя слияние обычных соседних абзацев на одной странице (не только через распорки). Guard `prevIsMergeable`/`nextIsMergeable`: сливаем только если сосед — element, не `HR` и не `EDITOR_ATOMIC_TAGS` (img/table/figure — им слияние абзацев неуместно, отдаём дефолту браузера). Слияние идёт через существующий `mergeAcrossBoundary`, который сохраняет формат ПРИЁМНИКА (Backspace → предыдущий блок, Delete → текущий), как в Word.
  - **4.14** Новая ветка Enter (без Shift) в `handleEditorKeyDown` перед Tab: если каретка в пустом `<li>` (нет текста, нет img/table) → `preventDefault` + `execCmd("outdent")` (понижает уровень вложенного пункта либо выносит из списка обычным блоком). Непустые пункты по-прежнему обрабатывает нативный split. Tab/Shift+Tab уровня уже были в Этапе 1.
  - `tsc -b` чисто. Deps `handleEditorKeyDown` не менялись (execCmd/commitHistoryNow/syncEditorAfterDomEdit уже были). Ручная QA под логином: Backspace/Delete слияние абзацев с сохранением формата приёмника; отсутствие залипания каретки в пустых inline; Enter на пустом пункте списка (верхний уровень и вложенный); отсутствие регрессий на границах страниц/атомарных блоках/hr.
  - Следующий шаг при «продолжай» — **Этап 3, задача 4.15** (индикация кнопок по queryCommandState через selectionchange).
- 2026-07-22 (сессия 6): **изменена логика удаления при вводе символов после/внутри TAB**:
  - Убрано атомарное удаление всего спана `data-tab` вместе с набранным текстом по Backspace и Delete.
  - Теперь все символы (как набранные вручную буквы, так и символы/пробелы табуляции) удаляются строго посимвольно (по одному символу за одно нажатие Backspace/Delete).
  - В `cleanEditorArtifacts` добавлена подчистка пустых спанов `[data-tab]`, когда все неразрывные пробелы табуляции удалены.

## Открытые вопросы к заказчику (блокируют часть задач)
- 4.3: приемлемо ли «табулятор фиксированной ширины» вместо истинных позиций табуляции Word?
- 4.13: plain-вставка — новый абзац на каждый \n или только на \n\n?
- 4.17: какой объём автозамен допустим в официальной переписке (кавычки-ёлочки, авто-тире)?
